const express = require("express");

function group(basePath, routesCallback) {
    const router = express.Router();
    routesCallback(router);
    return express.Router().use(basePath, router);
}

module.exports = group;